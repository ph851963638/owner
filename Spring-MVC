浏览器（输入网址）      服务器（调用服务器中java项目的代码）

MVC：
M:model 模型 （实体类）
表示项目间的模型数据

V:view   视图（html文件）
C: Controller 控制器（接收客户端请求的一种Java类）
    有一个类，在类名上面需要加一个注解@Controller；
    如果给类名的上面继续加一个注解@RequestMapping,就等于在定义客户端怎样的网址能够访问到网页；
控制器类：
    我们的目的是希望通过客户端的网址访问，能够调用控制器类中的方法，由此定义方法
    如果给类中的控制器增加一个注解@ResponseBody的话，就意味着该方法的返回值会直接给客户端进行显示
    反之，如果未加@ResponseBody,会直接将请求转发给一个视图文件
    
 电影的信息：
 电影名：   死侍
 电影类型： 科幻
 票房：     10亿 
    
对象的方式来表示数据，通过对象的属性来依次表示它的若干条数据

注意观察网址:   
